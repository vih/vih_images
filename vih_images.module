<?php

include_once('vih_images.features.inc');

/**
 * Implementation of hook_init()
 */
function vih_images_init() {

  // Adding the panopoly images file to the theme so it will show up in the WYSIWYG
  drupal_add_css(drupal_get_path('module', 'vih_images') . '/vih-images.css', array('group' => CSS_THEME));
}
